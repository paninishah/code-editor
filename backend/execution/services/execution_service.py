"""
Legacy Judge0 client is no longer used.

Code execution now runs locally via subprocesses in `runner.py`,
so this module is intentionally left as a no-op to avoid any
outbound network calls.
"""

